{
	"dependencies": {
		"@codeeditorland/counter": "^0.1.3"
	},
	"description": "SWC plugin for jest",
	"devDependencies": {},
	"main": "swc_plugin_jest.wasm",
	"name": "@codeeditorland/plugin-jest",
	"preferUnplugged": true,
	"scripts": {
		"prepack": "cargo build --release -p swc_plugin_jest --target wasm32-wasi && cp ../../target/wasm32-wasi/release/swc_plugin_jest.wasm ."
	}
}
